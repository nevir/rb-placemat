module <%= project_namespace %>
  # Version information for <%= project_namespace %>.
  module Version
    MAJOR = 0
    MINOR = 1
    PATCH = 0

    # The version number as a string.
    def self.to_s
      "#{MAJOR}.#{MINOR}.#{PATCH}"
    end
  end
end
